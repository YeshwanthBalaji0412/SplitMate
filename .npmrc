; Expo + React Native expect transitive deps (react-refresh, @babel/runtime,
; metro internals) to be resolvable from anywhere in the tree. pnpm's strict
; isolation hides them. node-linker=hoisted lays out node_modules in the same
; flat shape npm/yarn produce, which is what the Expo toolchain assumes.
node-linker=hoisted
