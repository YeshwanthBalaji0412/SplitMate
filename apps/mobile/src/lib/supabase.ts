// TODO(Phase 3): wire up the real Supabase client.
// This file is a placeholder so future imports from '@/lib/supabase' resolve.
// Phase 3 will replace this with createClient(supabaseUrl, anonKey, { auth: { storage: SecureStore adapter, ... } }).

export const supabase = null as unknown as never;
